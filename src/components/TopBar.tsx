import styled from "styled-components";

export const TopBar = () => {
  return <Container>I'm Top Bar</Container>;
};

const Container = styled.div`
  background-color: black;
  color: white;
  width: 100%;
  position: fixed;
`;
